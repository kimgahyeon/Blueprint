<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
  <meta name="description" content="">
  <meta name="author" content="">
  <link rel="icon" href="../../image/bitcoin.png">

  <title>Blueprint Repair List</title>

  <link href="../../bootstrap-4.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    @font-face {
      font-family: '나눔 스퀘어';
      src: url('../../font/NanumSquareRoundL.ttf') format('truetype')
    }

    body {
      font-family: "나눔 스퀘어";
      font-size: 120%;
    }
  </style>

</head>

<body>
  <h2 class="sub-header"><br><strong>&nbsp;&nbsp;수리 대기 주문</strong><br></h2>
  <div class="table-responsive">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Index</th>
          <th>Order Num</th>
          <th>Recept</th>
          <th>Token Price</th>
          <th>Accept</th>
        </tr>
      </thead>
      <tbody>
        <form action= '/Clear_Repair' method="POST">
        <% for (var i = 0;i < o_num.length;i++){ %>
          <tr>
            <td><%=i + 1%></td>
            <td><%=o_num[i]%></td>
            <td><%=recept[i]%></td>
            <td><%=price[i]%></td>
            <td><button type="submit" name = "number" value = <%=o_num[i]%> class="btn btn-success">Accept</button></td>
          </tr>
        <% } %>
      </form>
      </tbody>
    </table>
  </div>
  </div>
  </div>
  </div>

  <!-- Bootstrap core JavaScript
    ================================================== -->
  <!-- Placed at the end of the document so the pages load faster -->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
  <script src="../../bootstrap-4.1.3/dist/js/bootstrap.min.js"></script>

</body>

</html>